<UserControl x:Class="RobotController.UI.Views.Controls.OverridePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="120" d:DesignWidth="300">

    <Border Background="#2D2D2D" CornerRadius="8" Padding="12">
        <StackPanel>
            <!-- Header -->
            <TextBlock Text="OVERRIDE" FontSize="12" FontWeight="Bold"
                      Foreground="#AAAAAA" Margin="0,0,0,8"/>

            <!-- Override Sliders -->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>

                <!-- Program Override -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Program"
                          Foreground="#888888" VerticalAlignment="Center"
                          FontSize="11" Margin="0,4"/>
                <Slider Grid.Row="0" Grid.Column="1"
                       Minimum="1" Maximum="100"
                       Value="{Binding ProgramOverride, Mode=TwoWay}"
                       VerticalAlignment="Center" Margin="8,4"/>
                <TextBlock Grid.Row="0" Grid.Column="2"
                          Text="{Binding ProgramOverride, StringFormat='{}{0}%'}"
                          Foreground="White" FontFamily="Consolas"
                          TextAlignment="Right" VerticalAlignment="Center"/>

                <!-- Jog Override -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Jog"
                          Foreground="#888888" VerticalAlignment="Center"
                          FontSize="11" Margin="0,4"/>
                <Slider Grid.Row="1" Grid.Column="1"
                       Minimum="1" Maximum="100"
                       Value="{Binding JogOverride, Mode=TwoWay}"
                       VerticalAlignment="Center" Margin="8,4"/>
                <TextBlock Grid.Row="1" Grid.Column="2"
                          Text="{Binding JogOverride, StringFormat='{}{0}%'}"
                          Foreground="White" FontFamily="Consolas"
                          TextAlignment="Right" VerticalAlignment="Center"/>

                <!-- Manual Override (T1 mode cap) -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Manual"
                          Foreground="#888888" VerticalAlignment="Center"
                          FontSize="11" Margin="0,4"/>
                <Slider Grid.Row="2" Grid.Column="1"
                       Minimum="1" Maximum="100"
                       Value="{Binding ManualOverride, Mode=TwoWay}"
                       VerticalAlignment="Center" Margin="8,4"/>
                <TextBlock Grid.Row="2" Grid.Column="2"
                          Text="{Binding ManualOverride, StringFormat='{}{0}%'}"
                          Foreground="White" FontFamily="Consolas"
                          TextAlignment="Right" VerticalAlignment="Center"/>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
