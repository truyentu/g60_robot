<UserControl x:Class="RobotController.UI.Views.Controls.ModeSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="350">

    <UserControl.Resources>
        <Style x:Key="ModeRadioButton" TargetType="RadioButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="4"
                                Padding="12,10"
                                Margin="0,4"
                                Background="{StaticResource SecondaryBackground}"
                                Cursor="Hand">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#333337"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="{StaticResource PrimaryBackground}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="4"
            Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                <TextBlock Text="Operation Mode"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource ForegroundBrush}"/>
                <Border Background="{Binding ModeColor}"
                        CornerRadius="4"
                        Padding="8,4"
                        Margin="12,0,0,0">
                    <TextBlock Text="{Binding CurrentMode}"
                               FontWeight="Bold"
                               Foreground="Black"/>
                </Border>
            </StackPanel>

            <!-- Current mode info -->
            <Border Grid.Row="1"
                    Background="#2D2D30"
                    CornerRadius="4"
                    Padding="12"
                    Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0">
                        <TextBlock Text="Max Linear" FontSize="10" Foreground="#888"/>
                        <TextBlock FontSize="14" Foreground="{StaticResource ForegroundBrush}">
                            <Run Text="{Binding MaxLinearVelocity, StringFormat=N0}"/>
                            <Run Text="mm/s"/>
                        </TextBlock>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="1">
                        <TextBlock Text="Max Joint" FontSize="10" Foreground="#888"/>
                        <TextBlock FontSize="14" Foreground="{StaticResource ForegroundBrush}">
                            <Run Text="{Binding MaxJointVelocity, StringFormat=N0}"/>
                            <Run Text="deg/s"/>
                        </TextBlock>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,8,0,0">
                        <TextBlock Text="Deadman" FontSize="10" Foreground="#888"/>
                        <TextBlock Text="{Binding DeadmanRequired, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter='Required|Not Required'}"
                                   FontSize="12"
                                   Foreground="{Binding DeadmanRequired, Converter={StaticResource BoolToColor}, ConverterParameter='#FFA500|#888'}"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,8,0,0">
                        <TextBlock Text="Safety Fence" FontSize="10" Foreground="#888"/>
                        <TextBlock Text="{Binding SafetyFenceRequired, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter='Required|Not Required'}"
                                   FontSize="12"
                                   Foreground="{Binding SafetyFenceRequired, Converter={StaticResource BoolToColor}, ConverterParameter='#FFA500|#888'}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Mode selection list -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding AvailableModes}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <RadioButton Style="{StaticResource ModeRadioButton}"
                                         GroupName="OperationModes"
                                         IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                         Command="{Binding DataContext.SelectModeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                         CommandParameter="{Binding}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0"
                                            Width="6"
                                            CornerRadius="3"
                                            Background="{Binding Color}"
                                            Margin="0,0,12,0"/>

                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding DisplayName}"
                                                   FontWeight="SemiBold"
                                                   FontSize="14"
                                                   Foreground="{StaticResource ForegroundBrush}"/>
                                        <TextBlock Text="{Binding Description}"
                                                   FontSize="11"
                                                   Foreground="#888"
                                                   TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Grid>
                            </RadioButton>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Requirements section -->
            <Border Grid.Row="3"
                    Background="#2D2D30"
                    CornerRadius="4"
                    Padding="12"
                    Margin="0,12,0,0"
                    Visibility="{Binding SelectedMode, Converter={StaticResource NotNullToBool}}">
                <StackPanel>
                    <TextBlock Text="Requirements for selected mode:"
                               FontSize="12"
                               Foreground="#AAA"
                               Margin="0,0,0,8"/>

                    <ItemsControl ItemsSource="{Binding MissingRequirements}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="0,2">
                                    <TextBlock Text="X"
                                               Foreground="#FF6B6B"
                                               FontWeight="Bold"
                                               Margin="0,0,8,0"/>
                                    <TextBlock Text="{Binding}"
                                               Foreground="#FF6B6B"/>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock Text="All requirements met"
                               Foreground="#4CAF50"
                               Visibility="{Binding CanSwitchMode, Converter={StaticResource BoolToVisibility}}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding MissingRequirements.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <!-- Error message -->
                    <TextBlock Text="{Binding SwitchError}"
                               Foreground="#FF6B6B"
                               FontSize="11"
                               TextWrapping="Wrap"
                               Margin="0,8,0,0"
                               Visibility="{Binding SwitchError, Converter={StaticResource StringToVisibilityConverter}}"/>
                </StackPanel>
            </Border>

            <!-- Switch button -->
            <Button Grid.Row="4"
                    Content="Switch Mode"
                    Command="{Binding SwitchModeCommand}"
                    IsEnabled="{Binding CanSwitchMode}"
                    Margin="0,12,0,0"
                    Padding="16,10"
                    FontWeight="SemiBold"
                    Background="{StaticResource AccentBrush}"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="4"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter Property="Background" Value="#555"/>
                                            <Setter Property="Foreground" Value="#888"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#0095E8"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</UserControl>
