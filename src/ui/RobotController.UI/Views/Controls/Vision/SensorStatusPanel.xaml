<UserControl x:Class="RobotController.UI.Views.Controls.Vision.SensorStatusPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">

    <Border Background="#D0D0D0" CornerRadius="8" Padding="12">
        <StackPanel>
            <TextBlock Text="SENSOR STATUS" FontSize="12" FontWeight="Bold"
                      Foreground="#888888" Margin="0,0,0,12"/>

            <!-- Connection status -->
            <Grid Margin="0,0,0,8">
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="12" Height="12"
                            Fill="{Binding StatusColor}" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding ConnectionStatus}"
                              Foreground="#333333"/>
                </StackPanel>
            </Grid>

            <!-- Device selection -->
            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ComboBox Grid.Column="0"
                         ItemsSource="{Binding AvailableDevices}"
                         SelectedItem="{Binding SelectedDevice}"/>
                <Button Grid.Column="1" Content="R" Width="30" Margin="4,0,0,0"
                       Command="{Binding EnumerateDevicesCommand}" ToolTip="Refresh"/>
            </Grid>

            <!-- Connect/Disconnect buttons -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                <Button Content="Connect" Padding="16,4" Margin="0,0,4,0"
                       Command="{Binding ConnectCommand}"
                       IsEnabled="{Binding IsConnected, Converter={StaticResource InverseBooleanConverter}}"/>
                <Button Content="Disconnect" Padding="16,4"
                       Command="{Binding DisconnectCommand}"
                       IsEnabled="{Binding IsConnected}"/>
            </StackPanel>

            <Rectangle Height="1" Fill="#999999" Margin="0,0,0,12"/>

            <!-- Device info -->
            <StackPanel Visibility="{Binding IsConnected, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Grid Margin="0,0,0,4">
                    <TextBlock Text="Model:" Foreground="#888888"/>
                    <TextBlock Text="{Binding ModelName}" Foreground="#333333"
                              HorizontalAlignment="Right"/>
                </Grid>

                <Grid Margin="0,0,0,4">
                    <TextBlock Text="Serial:" Foreground="#888888"/>
                    <TextBlock Text="{Binding SerialNumber}" Foreground="#333333"
                              HorizontalAlignment="Right"/>
                </Grid>

                <Grid Margin="0,0,0,4">
                    <TextBlock Text="Frame Rate:" Foreground="#888888"/>
                    <TextBlock Text="{Binding FrameRateDisplay}" Foreground="#333333"
                              HorizontalAlignment="Right"/>
                </Grid>

                <Grid Margin="0,0,0,4">
                    <TextBlock Text="Frames:" Foreground="#888888"/>
                    <TextBlock Text="{Binding FramesDisplay}" Foreground="#333333"
                              HorizontalAlignment="Right"/>
                </Grid>

                <Grid Margin="0,0,0,8">
                    <TextBlock Text="Dropped:" Foreground="#888888"/>
                    <TextBlock Text="{Binding DroppedDisplay}" Foreground="#FF6666"
                              HorizontalAlignment="Right"/>
                </Grid>

                <!-- Acquisition controls -->
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <Button Content="Start" Padding="16,4" Margin="0,0,4,0"
                           Command="{Binding StartAcquisitionCommand}"
                           IsEnabled="{Binding IsAcquiring, Converter={StaticResource InverseBooleanConverter}}"/>
                    <Button Content="Stop" Padding="16,4"
                           Command="{Binding StopAcquisitionCommand}"
                           IsEnabled="{Binding IsAcquiring}"/>
                </StackPanel>
            </StackPanel>

            <Rectangle Height="1" Fill="#999999" Margin="0,12"/>

            <!-- Configuration -->
            <Expander Header="Configuration" Foreground="#888888">
                <StackPanel Margin="0,8,0,0">
                    <Grid Margin="0,0,0,4">
                        <TextBlock Text="Exposure (us):" Foreground="#888888"/>
                        <TextBox Text="{Binding ExposureTime}" Width="80"
                                HorizontalAlignment="Right"/>
                    </Grid>

                    <Grid Margin="0,0,0,4">
                        <TextBlock Text="Gain:" Foreground="#888888"/>
                        <TextBox Text="{Binding Gain}" Width="80"
                                HorizontalAlignment="Right"/>
                    </Grid>

                    <Grid Margin="0,0,0,4">
                        <TextBlock Text="Trigger:" Foreground="#888888"/>
                        <ComboBox ItemsSource="{Binding TriggerModes}"
                                 SelectedItem="{Binding TriggerMode}"
                                 Width="100" HorizontalAlignment="Right"/>
                    </Grid>

                    <Button Content="Apply" Padding="16,4" Margin="0,8,0,0"
                           Command="{Binding ApplyConfigCommand}"
                           HorizontalAlignment="Right"/>
                </StackPanel>
            </Expander>
        </StackPanel>
    </Border>
</UserControl>
