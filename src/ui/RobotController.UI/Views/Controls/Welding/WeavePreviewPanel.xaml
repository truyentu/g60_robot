<UserControl x:Class="RobotController.UI.Views.Controls.Welding.WeavePreviewPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="250">

    <Border Background="#D0D0D0" CornerRadius="8" Padding="12">
        <StackPanel>
            <TextBlock Text="WEAVE PREVIEW"
                      FontSize="12" FontWeight="Bold"
                      Foreground="#888888" Margin="0,0,0,8"/>

            <!-- Pattern Preview Canvas -->
            <Border Background="#1A1A1A" CornerRadius="4" Height="80" Margin="0,0,0,8">
                <Canvas ClipToBounds="True">
                    <Polyline Points="{Binding PreviewPoints}"
                             Stroke="#00AAFF" StrokeThickness="2"
                             StrokeLineJoin="Round"/>
                    <!-- Center line -->
                    <Line X1="0" Y1="40" X2="200" Y2="40"
                         Stroke="#404040" StrokeThickness="1"
                         StrokeDashArray="4,2"/>
                </Canvas>
            </Border>

            <!-- Pattern Selection -->
            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Pattern:"
                          Foreground="#888888" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox Grid.Column="1"
                         ItemsSource="{Binding PatternTypes}"
                         SelectedItem="{Binding SelectedPattern}"
                         Background="#B8B8B8" Foreground="#333333"/>
            </Grid>

            <!-- Parameters -->
            <Grid Margin="0,0,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,4,0">
                    <TextBlock Text="Amplitude" Foreground="#888888" FontSize="10"/>
                    <TextBox Text="{Binding Amplitude, UpdateSourceTrigger=PropertyChanged}"
                            Background="#B8B8B8" Foreground="#333333" Padding="4"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="4,0,0,0">
                    <TextBlock Text="Wavelength" Foreground="#888888" FontSize="10"/>
                    <TextBox Text="{Binding Wavelength, UpdateSourceTrigger=PropertyChanged}"
                            Background="#B8B8B8" Foreground="#333333" Padding="4"/>
                </StackPanel>
            </Grid>

            <!-- Dwell times -->
            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,4,0">
                    <TextBlock Text="Dwell Left (ms)" Foreground="#888888" FontSize="10"/>
                    <TextBox Text="{Binding DwellLeft}"
                            Background="#B8B8B8" Foreground="#333333" Padding="4"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="4,0,0,0">
                    <TextBlock Text="Dwell Right (ms)" Foreground="#888888" FontSize="10"/>
                    <TextBox Text="{Binding DwellRight}"
                            Background="#B8B8B8" Foreground="#333333" Padding="4"/>
                </StackPanel>
            </Grid>

            <!-- Presets -->
            <TextBlock Text="Presets:" Foreground="#888888" FontSize="10" Margin="0,0,0,4"/>
            <ItemsControl ItemsSource="{Binding Presets}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding}"
                               Command="{Binding DataContext.ApplyPresetCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                               CommandParameter="{Binding}"
                               Margin="2" Padding="8,4"
                               Background="#B8B8B8" Foreground="#333333"
                               FontSize="10"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Enable/Status -->
            <Grid Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <CheckBox Grid.Column="0" Content="Enable Weave"
                         IsChecked="{Binding IsEnabled}"
                         Foreground="#333333"/>

                <TextBlock Grid.Column="1"
                          Text="{Binding PathLengthRatio, StringFormat=Path: {0:F2}x}"
                          Foreground="#888888" FontSize="10"/>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
