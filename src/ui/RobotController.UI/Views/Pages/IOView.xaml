<UserControl x:Class="RobotController.UI.Views.Pages.IOView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="Transparent">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="I/O MONITOR" FontSize="18" FontWeight="Bold"
                   Foreground="#222222" Margin="0,0,0,16"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Digital Inputs -->
            <Border Grid.Column="0" Background="#D8D8D8" CornerRadius="8" Margin="0,0,8,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="DIGITAL INPUTS" FontWeight="Bold"
                               Foreground="#555555" Margin="16,12"/>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding DigitalInputs}" Margin="16,0,16,16">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#545454" CornerRadius="4" Padding="12,8" Margin="0,2">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="60"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Column="0" Text="{Binding Address}"
                                                       Foreground="#666666" FontFamily="Consolas"/>
                                            <TextBlock Grid.Column="1" Text="{Binding Name}"
                                                       Foreground="#222222"/>
                                            <Ellipse Grid.Column="2" Width="16" Height="16">
                                                <Ellipse.Style>
                                                    <Style TargetType="Ellipse">
                                                        <Setter Property="Fill" Value="#5E5E5E"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Value}" Value="True">
                                                                <Setter Property="Fill" Value="#00FF00"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Ellipse.Style>
                                            </Ellipse>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Digital Outputs -->
            <Border Grid.Column="1" Background="#D8D8D8" CornerRadius="8" Margin="8,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="DIGITAL OUTPUTS" FontWeight="Bold"
                               Foreground="#555555" Margin="16,12"/>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding DigitalOutputs}" Margin="16,0,16,16">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#545454" CornerRadius="4" Padding="12,8" Margin="0,2">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="60"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Column="0" Text="{Binding Address}"
                                                       Foreground="#666666" FontFamily="Consolas"/>
                                            <TextBlock Grid.Column="1" Text="{Binding Name}"
                                                       Foreground="#222222"/>
                                            <ToggleButton Grid.Column="2" Width="50" Height="24"
                                                          IsChecked="{Binding Value}"
                                                          Command="{Binding DataContext.ToggleOutputCommand,
                                                                    RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                          CommandParameter="{Binding}">
                                                <ToggleButton.Style>
                                                    <Style TargetType="ToggleButton">
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="ToggleButton">
                                                                    <Border x:Name="border" CornerRadius="12"
                                                                            Background="#5E5E5E">
                                                                        <Grid>
                                                                            <TextBlock x:Name="offText" Text="OFF"
                                                                                       Foreground="#666666"
                                                                                       HorizontalAlignment="Right"
                                                                                       VerticalAlignment="Center"
                                                                                       Margin="0,0,8,0" FontSize="10"/>
                                                                            <Ellipse x:Name="toggle" Width="18" Height="18"
                                                                                     Fill="White" HorizontalAlignment="Left"
                                                                                     Margin="3,0,0,0"/>
                                                                        </Grid>
                                                                    </Border>
                                                                    <ControlTemplate.Triggers>
                                                                        <Trigger Property="IsChecked" Value="True">
                                                                            <Setter TargetName="border" Property="Background" Value="#00AA00"/>
                                                                            <Setter TargetName="toggle" Property="HorizontalAlignment" Value="Right"/>
                                                                            <Setter TargetName="toggle" Property="Margin" Value="0,0,3,0"/>
                                                                            <Setter TargetName="offText" Property="Text" Value="ON"/>
                                                                            <Setter TargetName="offText" Property="HorizontalAlignment" Value="Left"/>
                                                                            <Setter TargetName="offText" Property="Margin" Value="8,0,0,0"/>
                                                                            <Setter TargetName="offText" Property="Foreground" Value="White"/>
                                                                        </Trigger>
                                                                    </ControlTemplate.Triggers>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </ToggleButton.Style>
                                            </ToggleButton>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
